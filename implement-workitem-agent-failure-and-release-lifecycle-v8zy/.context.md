# Goal
Make one WorkItemWorkflow claim, run the agent, and release cleanly on failure or cancellation.

# Scope
- `src/workflows/work-item.ts` and lifecycle Activities required for claim, release, cleanup, and query state.

# Implementation
Keep orchestration deterministic. Put backend, workspace, filesystem, and process effects in Activities.

# Acceptance Criteria
- Agent success reaches the documented terminal phase.
- Agent failure records durable failed state and releases the backend task.
- Cancellation and wont-do release the task and clean up Activity-owned resources.
- Query state exposes task ID, phase, workspace, feedback round, and failure reason.

# Tests
- Add focused WorkItemWorkflow and Activity tests.
- Run `npx tsx --test src/workflows/work-item.test.ts` and `npm run build`.

# Dependencies
- ExecuteAgent Activity.
- yx task backend adapter.

# Non-Goals
- Pull request review and merge lifecycle.

## Needs Refinement

Flagged by autonomous agent on 2026-09-12.

**Reason:** Design divergence, not missing context: this yak and docs/plans/2026-09-05-task-dispatch-design.md:116 say WorkItemWorkflow must 'claim and release the task', but WorkDispatcherWorkflow (src/workflows/dispatcher.ts:61,71,76) already owns claim/markDone/recordFailure and work-item.ts performs neither. Implementing the yak as written moves claim/release into the workflow, which would double-claim and conflict with the dispatcher. A human must decide the single owner of claim/release/markDone (dispatcher vs workflow) before this is implementable. Note: the @implementation-failed tag here is also stale — no '## Implementation attempt' section exists in context (the real failure path in src/backends/yx.ts:154 always appends one), so it likely comes from test pollution rather than a genuine failure.

To unblock: add Goal, Acceptance Criteria with checkboxes, and specific file paths.
